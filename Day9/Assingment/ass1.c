/*
        Using For loop & While loop
        Write a program to display n terms of Fibonacci series
        Input: 6
        Output: 1, 1, 2, 3, 5, 8
*/

#include <stdio.h>

int main()
{

    /*
        int n1 = 0, n2 = 1, n3, i, number;
        printf("Enter the number of elements:");
        scanf("%d", &number);
        printf("\n%d %d", n1, n2);   // printing 0 and 1
        for (i = 2; i < number; ++i) // loop starts from 2 because 0 and 1 are already printed
        {
            n3 = n1 + n2;
            printf(" %d", n3);
            n1 = n2;
            n2 = n3;
        }


    enum choice
    {
        n1,
        n2,
        n3
    };
    enum choice e1, e, e3;
    printf("%d", sizeof(enum choice));

*/
    typedef int double_new;
    typedef double_new double_old;
    double_old new = 1111;
    double_new old = 2222;
    printf("%d\n", old );
     printf("%d",  new);

    return 0;
}